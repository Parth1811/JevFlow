# Jevflow build log

## Cycle 0 (2026-09-24 05:10 UTC, setup)
- Spec, tasks, loop state, builder/critic prompts written. Hooks contract verified from docs: Stop supports `{"decision":"block","reason"}`, `stop_hook_active`, `last_assistant_message`, 8 consecutive block cap; SessionStart supports `additionalContext`; plugin hooks in `hooks/hooks.json` with `${CLAUDE_PLUGIN_ROOT}`. Host has claude 2.1.280 with `--plugin-dir`, git, jq, /usr/bin/python3.11.

## Cycle 0b (2026-09-24 05:10 UTC, scope update)
- Added SPEC section 10 (research-derived flows and conditions). Core items folded into B2-D1; extensions moved to phase F; README + report moved to G1. 16 tasks total.

## Cycle 1 (2026-09-24 05:45 UTC, A1 + A2)
- A1 done: spike plugin `examples/spike/plugin/` loaded via `--plugin-dir`; 3 real `claude -p` runs (budget: 1 left for E2). Captured SessionStart startup/resume/compact, Stop (first + stop_hook_active=true after a block), PreCompact. Block via `{"decision":"block"}` confirmed; `/compact` in `-p` fires SessionStart source=compact. StopFailure not triggerable safely; fixture taken from the hooks reference. Verdict: `claude -p` runs unattended here (permission_mode auto); E2 should pass explicit `--permission-mode acceptEdits`. `cwd` is the realpath (`/local/home/...`). Sanitized fixtures in `tests/fixtures/hooks/` (7 files).
- A2 done: `loop/spikes/a2_jev_spike.py`, real Jev calls (jev-1.13.0, 270-520 ms). 3 wordings x 4 synthetic cases. Chosen v3 (hybrid) judges all 4 correctly: phase_complete verify 0.83, stuck_looping stuck 0.95, premature_done claims_done 0.93 with verify 0.03. Bands: auto >= 0.80, review 0.50-0.80, drop < 0.50; stuck/off_goal 0.70. Added `claims_done` noul to SPEC 3; next_action is weakest (0.58-0.66), policy must not key on it. Details in docs/RESEARCH.md.
- critic: 2 issues found, 2 fixed (raw spike logs with unsanitized host paths would have been committed, now gitignored; SPEC 3 missing claims_done, added). Checked: no em-dashes, no key material in outputs, hook scripts always exit 0.

## Cycle 2 (2026-09-24 06:10 UTC, B1 + B2)
- B1 done: `jevflow/jev_client.py` (stdlib). Key resolution JEV_API_KEY > JEVFLOW_KEY_FILE > ~/.config/jevflow/api_key; retry with exponential backoff on 429/529/5xx, Retry-After honoured and capped at 8s; typed errors (JevKeyError, JevBudgetError, JevTimeoutError, JevConnectionError, JevHTTPError, JevResponseError, all JevError); `max_calls` counts one per ask() (retries do not recount, failed calls do); key redacted from error bodies and hidden from repr. 21 unit tests with scripted urlopen. One real synthetic call made to confirm the client against the live API (noul 0.96, 1 call).
- B2 done: `jevflow/flow.py` (strict validation: unknown keys rejected, id regex, reserved `unclear`, schema_version/flow_version, linear-default depends_on with explicit override, DAG cycle detection naming the cycle, loop, on_fail, limits + confidence bands defaults from A2, privacy, mode) and `jevflow/state.py` (new/load/validate with flow-change reconciliation journaled as `flow_changed`, atomic save via mkstemp+fsync+os.replace, `record()` persists the journal entry before returning, history cap 500). 38 unit tests incl. invalid flows, a 3-node DAG cycle, torn-write and concurrent-writer checks.
- Total suite: 59 tests, all pass on /usr/bin/python3.11.
- Note: flow `mode` defaults to `enforce` until F1 (shadow mode) lands; SPEC 10.3 suggests `warn` for new flows, which /jevflow:init (C2) can write explicitly.
- critic: 5 issues found, 5 fixed (key with CR/LF or spaces could inject into the Authorization header, now rejected; malformed `loop_iterations` would raise AttributeError instead of StateError; key tests clobbered HOME unsafely, now mock.patch.dict; one wrong test expectation on self-dependency; one vacuous repr assertion removed). Checked: no em-dashes, no personal paths in jevflow/, key never printed.

## Cycle 3 (2026-09-24 06:40 UTC, B3 + B4)
- B3 done: `jevflow/judge.py`. `build_state` curates goal, phase table with status, check results (failing output tail), last message tail (4k), change summary, last 5 decisions; trims through 6 levels to `limits.state_char_budget` (verified <= budget at 12000/6000/3000/1500, always valid JSON, goal and phase ids never dropped). v3 question set from A2, `unclear` on every choice, no why/attribution questions. Compete-then-verify: second `noul` call on the `current_phase` winner, skipped on `unclear`. `privacy.send_diff=false` sends only `path +A -R` (test proves diff text is dropped). Any JevError or malformed/non-finite answer yields a degraded Judgment; `judge()` never raises. 22 tests.
- B4 done: `jevflow/policy.py`, pure `decide()` plus `apply_decision()`. Fixed priority order: already_done, budgets (blocks, time, jev calls, Claude consecutive-block cap via stop_hook_active), regression, bounded loop (continue / pass / exhausted to ask_human or on_fail), degraded checks-only, ask_human (auto conf only), stuck + escalation (streak 2 or same failure 3x, third escalation asks a human), off_goal, on_fail branch / premature completion, unclear abstain, compete-then-verify advance (winner conf AND verify >= auto AND check passes), review / drop / phase_mismatch bands. Advance follows DAG eligibility. Added `Flow.branch_only` / `required()`: an on_fail target nothing depends on (e.g. `debug`) is never offered normally and not required for the goal. State gained consecutive_blocks, stuck_streak, escalations, same_reason_count, last_failure, needs_human. 37 table cases (27 distinct conditions) + 16 detail tests; decide() verified not to mutate state.
- Live check (synthetic premature_done case, 2 Jev calls): claims_done 0.93, verify 0.03, check failing -> BLOCK premature_completion.
- Total suite: 96 tests, all pass on /usr/bin/python3.11.
- critic: 7 issues found, 7 fixed (max_blocks_per_session was silently capped at Claude's 8; identical "continue" reasons on a long no-check phase escalated as looping, now only an identical failing check counts; last phase done with an earlier check failing routed to ask_human, now blocks with final_check_fail; NaN/inf Jev numbers passed validation and compared False against every band, now degrade; a branch-only phase check was treated as a standing regression invariant; privacy docstring did not state that check output is sent; 3 ResourceWarnings from unclosed files in tests). Checked: no em-dashes, no personal paths in jevflow/, key never printed.

## Cycle 4 (2026-09-24 07:05 UTC, C1 + C2)
- C1 done: `.claude-plugin/plugin.json`, `hooks/hooks.json` (SessionStart 30s, Stop 600s, StopFailure 30s), `hooks/jevflow` POSIX launcher (probes candidates by requiring a printed marker, so the silent python3 shim on this host is skipped; any crash in `hook` mode prints `{}` and exits 0, never exit 2), `jevflow/hooks.py`, `jevflow/project.py`, `jevflow/__main__.py`. SessionStart injects goal, phase table, current phase + check; resume/compact also re-inject the last BLOCK reason (truncated 1.5k); startup/resume/clear reset per-session block counters, compact does not. Stop runs checks (done phases + current + loop.until), probes `decide()` with no judgment first so budgets, caps, regression and loop phases never spend Jev calls, then judges and decides; journal written before output. StopFailure records `last_error` for the supervisor. Every failure path fails open (flow/state error, save error, internal bug, garbage stdin).
- C2 done: `commands/init.md` (flow format, writes `mode: warn`, validates), `commands/status.md`, `skills/jevflow/SKILL.md`, `python -m jevflow status [--json]` and `validate`. Status prints the phase table, budgets, last errors, last 8 decisions and NEEDS_HUMAN when present (tested on the A1 fixtures).
- Live check (synthetic toy project `loop/scratch/c1_live/`, real Jev through the real launcher, 8 Jev calls): "All done!" with the check failing -> BLOCK premature_completion (claims_done 0.96, verify 0.02). After the file was written with an honest message, Jev verify stayed in the review band (0.63, 0.71, 0.74), which would have blocked forever.
- Policy change from that finding (B4 amended): review band + passing defined check for 2 consecutive stops on the same phase -> ADVANCE `review_check_pass` with a note. Jev alone still never advances; phases without a check still never advance from the review band. Live rerun advanced on the 2nd stop. 4 new policy tests.
- Total suite: 152 tests, all pass on /usr/bin/python3.11 (52 new in tests/test_hooks.py incl. real launcher subprocess runs). Three mutation checks (launcher fallback, env scrub, deterministic probe) each made a test fail.
- Not verified without a real `claude` run (budget: 1 left, reserved for E2): `allowed-tools` pattern `Bash(${CLAUDE_PLUGIN_ROOT}/hooks/jevflow status:*)` in the commands, and `!` pre-execution in /jevflow:status.
- Shadow modes: hooks already honour `mode` (observe: silent, warn: systemMessage, enforce: block); block counters are not charged when not enforced. F1 still owns the policy-level tests.
- critic: 6 issues found, 6 fixed (a flow in a parent directory could capture an unrelated project, now CLAUDE_PROJECT_DIR is authoritative; checks 480s + git + Jev retries could exceed the 600s hook timeout and lose the journal, now checks 300s and a 45s git budget; Jev errors overwrote `last_error`, which the supervisor reads for StopFailure backoff, now `last_jev_error`; the Jev key env vars reached user check commands, now scrubbed; allowed-tools used a quoted pattern that does not match the documented syntax; a stray f-string). Checked: no em-dashes, no personal paths in shipped dirs (test enforces), key never printed.

## Cycle 5 (2026-09-24 07:40 UTC, D1 + E1)
- D1 done: `jevflow/supervisor.py` + `python -m jevflow run` (usage via `run -h`). Launches `claude -p <prompt> --plugin-dir <root> --output-format json [--resume sid] [--max-turns N] --permission-mode acceptEdits` in the project with its own process group; stdout/stderr kept per run in `.jevflow/runs/` (unique names, reruns never overwrite). After each exit it reloads state.json: done -> 0, needs_human -> 4 (never relaunched; deleting NEEDS_HUMAN.md and rerunning clears it, journaled `human_resolved`), time / Jev budget -> 2, restarts >= max_restarts -> 2, config errors (no flow in exactly that dir, bad flow, missing or non-executable binary, not a plugin dir) -> 3, live lease -> 5 (new code, state not touched). Restart prompt carries goal, current phase, done_when, check and the last BLOCK reason; resume uses the session_id from the JSON result, and a resumed run with no result starts fresh next time. Lease: `.jevflow/lock` with pid/host/ts plus an exclusive flock, so a lock left by a dead supervisor is taken over (journaled) and a live one is refused. Watchdog: newest of transcript mtime (from SessionStart), state.json, run output and launch time; idle >= `hang_minutes` kills the whole process group (SIGTERM, then SIGKILL) and relaunches, counted as a restart. A busy runaway child is killed at `max_total_minutes` + 60 s and ends with exit 2. StopFailure `rate_limit|overloaded|server_error` recorded after this launch -> backoff 30 s doubling to 600 s, capped by remaining time, not counted as a restart, 6 in a row -> exit 2; an older last_error is ignored. SIGTERM/Ctrl-C to the supervisor kills the child group, journals `supervisor_interrupted`, exits 130. Budget report printed (or `--json`) and written to `.jevflow/last_run.json`, plus a `supervisor_end` journal entry.
- Flow change: `limits.max_restarts` may now be 0 (run once, never relaunch); every other limit is still >= 1.
- E1 done: `tests/test_live_jev.py` (skips cleanly with no key, verified with HOME and key env unset). Live run with the key (synthetic premature-done state, 2 Jev calls, 0.6 s): current_phase test 1.0, next_action continue_phase 0.85, claims_done 0.92, phase_done__test 0.02, verify 0.02, stuck 0.20, off_goal 0.15 -> BLOCK premature_completion. Test asserts well-formed Judgment (not degraded, ids valid, all numbers finite in [0,1], 1-2 calls) and that policy blocks.
- Total suite: 179 tests, all pass on /usr/bin/python3.11 (1 skipped without key). 27 new supervisor tests using a scripted fake claude (restart after non-done, stop on done, max_restarts incl. persisted count and 0, hang kill incl. grandchild reaped, busy transcript is not a hang, overtime kill, live vs stale lease, two leases in one process, backoff 30/60 with no restart charged, backoff cap, stale last_error ignored, exit 4 paths, budgets before launch, config errors, parent flow does not capture a subdir, CLI exit codes and report, SIGTERM stops the child group). Suite run 3 more times, stable. Mutation checks (no new session, API errors counted as restarts, shared instead of exclusive flock) each made a test fail.
- claude_runs_used unchanged (3); the last real run stays reserved for E2.
- critic: 7 issues found, 7 fixed (a busy runaway child could outlive max_total_minutes since only idleness was watched; `max_restarts: 0` was rejected by flow validation; run logs were named 001.. per invocation so a rerun overwrote earlier evidence; an interrupt during backoff or a run left no journal entry; a `getattr`/ad hoc attribute for `--json` replaced by a RunConfig field; a first-prompt expression keyed on a nonexistent state field; no warning that JEV_API_KEY in the environment is inherited by the claude session, now warned without echoing it). Checked: no em-dashes, no personal paths in shipped dirs, key never printed (test asserts a fake key is absent from output).
- Known gap (for README): the hooks need the key, so the claude process inherits JEVFLOW_KEY_FILE (or JEV_API_KEY) and the agent, running as the same user, could read it. Prefer a key file; this is not a sandbox.

## Cycle 6 (2026-09-24 08:10 UTC, E2 + F1)
- E2 done: `examples/todo/.jevflow/flow.json` (5 phases: DAG with implement and docs both after scaffold, `test` loop max 3 with `on_fail: debug`, branch-only `debug`; enforce; `max_restarts: 0`). Log in docs/DEMO.md.
  - Run 1, the 4th and last real `claude -p` run (11 turns, 91 s, $0.59): all 7 writing tool calls were refused (4 Write "sensitive file", 3 Bash outside the allow list). Stop hook: scaffold check failing, Jev next_action ask_human 0.98 -> ALLOW_STOP ask_human, NEEDS_HUMAN.md written, supervisor exit 4 after 1.6 min. Correct handling, but no code was written. Unverified cause: project nested inside the `--plugin-dir` tree, which Claude Code's edit safety check protects. Needs one more real run with the project outside the plugin dir (next steps for the morning report).
  - Run 2 (SPEC 9.3 fallback): `examples/todo_replay/replay_claude.py` stands in for claude via JEVFLOW_CLAUDE_BIN. Real hooks, checks, Jev and supervisor. 6 stops: advance, advance, review_band block, review_check_pass advance, loop_continue block (false "all tests pass" claim, real failing assertion returned), goal_complete. Exit 0, 8 Jev calls, 4 s. The on_fail branch was not taken (loop passed on iteration 2); covered by unit tests only.
- Fixes from E2: supervisor counts and reports claude `permission_denials` (report line + supervisor_end journal); `jevflow run` accepts `--opt=value` (needed for `--claude-arg=--allowedTools`); git change summary uses `--relative` so a project nested in a larger repo lists only its own files.
- F1 done: `policy.apply_mode(decision, mode) -> ModeEffect` owns the ladder; hooks use it. enforce: blocks, ask_human pauses. warn: never blocks, systemMessage for a would-be block (an ADVANCE says the phase table moved), ask_human still pauses. observe: silent, ask_human journaled but no NEEDS_HUMAN and no pause. Shadow modes keep phase bookkeeping, never charge block counters. Input decision never mutated.
- Total suite: 189 tests, all pass on /usr/bin/python3.11 (1 skipped without key). New: 5 shadow-mode policy tests (12-row mode x kind table), 2 hook tests (observe/warn ask_human), git subdir scoping, `--opt=value` parsing, permission-denial reporting. Mutation check (observe keeps needs_human) failed 2 tests as expected.
- claude_runs_used: 4 of 4. No more real runs this night.
- critic: 5 issues found, 5 fixed (the first `jevflow run` attempt was launched from inside the project so `-m jevflow` was not importable and nothing ran; `--claude-arg=...` was rejected as an unknown option; the parent repo's uncommitted files leaked into Jev's change summary; the warn message for an ADVANCE said only "would block" although the phase table did move; demo outputs and runtime files (runs/, lock, last_run.json, replay logs) would have been committed, now moved to gitignored loop/scratch/e2_* and ignored at any depth). Checked: full key string absent from the repo, no em-dashes, no personal paths in shipped dirs.
- Fence note: during the mutation check a one-file backup of jevflow/policy.py was written to /tmp and then deleted (outside the fence, contents were repo code only). A `git checkout` in that step had reverted the file; it was restored from that backup and the suite rerun green.

## Cycle 7 (2026-09-24 08:40 UTC, F2 + F3)
- F2 done: `jevflow/gates.py` + `hooks.on_pre_tool_use`, PreToolUse matcher `Bash` (60 s timeout). Five nouls (destructive, remote_code, prod_scope, privileged, regenerable_artifacts) composed by a pure `compose_risk`: any non-destructive noul >= deny -> deny; destructive >= deny -> deny, or ask when regenerable >= 0.80; anything in [ask, deny) -> ask; else none. The gate never emits `allow`, so it can only tighten Claude Code's own permissions. Plain read-only commands (small allowlist, no shell metacharacters) and the plugin's own `hooks/jevflow status|validate` skip Jev. Jev failure, malformed answers or a spent `max_jev_calls` budget -> none (fail open to Claude Code's permission system). Gate Jev client capped at 10 s timeout, 1 retry. Modes: enforce applies ask/deny, warn gives a systemMessage, observe journals only.
- F3 done: `hooks.on_post_tool_use`, PostToolUse matcher `WebFetch|Read`. One `injection` noul on the extracted text of the tool response (8k chars); >= 0.70 adds a warning to Claude's context (enforce) or a systemMessage (warn). Never blocks. Read is screened only when listed in `gates.injection_tools` AND `privacy.send_diff` is true, so file contents never reach Jev by default.
- Flow: `gates` is now validated (`pre_tool`, `injection_screen` booleans default false, `injection_tools` from WebFetch/Read default [WebFetch], `bands` deny/ask/regenerable/injection in [0,1] with ask <= deny, unknown keys rejected).
- Live check (F2 acceptance, 6 real Jev calls, synthetic only): build-dir wipe -> ask (destructive 0.95, regenerable 0.82), `rm -rf src/ .git/` -> deny (0.99 / 0.04), `curl | sh` -> deny (remote_code 0.99), unittest -> none; injection benign 0.03, attack 0.99. Recorded in docs/RESEARCH.md and loop/spikes/f2_live.json.
- Total suite: 227 tests, all pass on /usr/bin/python3.11 (1 skipped without key). 38 new in tests/test_gates.py (13-row composition table, never-allow, band override, read-only allowlist, request shape, truncation, degraded paths, NaN, injection paths, response text extraction, flow validation, hook I/O per mode, budget spent, Read privacy rule, redaction sent and journaled, parallel hooks, launcher wrapper, hooks.json). Mutation checks (lock removed: parallel test failed 3 of 3 runs; `allow` emitted for low risk: 5 failures). Backups for the mutation checks kept inside the fence (loop/scratch/) and restored with cp, no git checkout.
- critic: 5 issues found, 5 fixed (commands with credentials were sent to Jev and journaled verbatim, now best-effort redaction of password/token/api_key/Bearer/AWS key ids in both; parallel Bash calls run PreToolUse hooks concurrently and lost jev_calls/journal updates, now a reload-and-record under an exclusive flock with the Jev call outside the lock; the plugin's own status command went to Jev; gate journal entries used `decision`, which would have polluted the judge's history view, now `verdict`; the default 15 s x 4 attempt Jev client could outlast the gate hook timeout, now 10 s x 2). Checked: no em-dashes, no personal paths in shipped dirs, key string absent from the repo.
- Known gap: redaction is pattern based; README must say not to enable the pre-tool gate when secrets appear on command lines.

## Cycle 8 (2026-09-24 09:25 UTC, F4 + F5)
- F4 done: `jevflow/regions.py`. Dynamic region: the agent writes `.jevflow/subtasks.json` (`{"<phase>": ["step", {"title", "done"}]}`); each Stop copies sub-steps of `dynamic` phases only into `state.subtasks` (ids s1..s12 by position, titles capped at 200 chars, file capped at 64 KB, bad JSON ignored, warnings journaled only when they change). Entries for other phases are ignored, so the skeleton cannot be edited. Jev gets a `current_subtask` choice (sub-steps + all_done + unclear); policy uses it only to HOLD an advance (`subtask_pending`, when a specific sub-step is named at `auto`), never to cause one, and ignores it in checks-only mode. Side-effect phases: `phase_attempts` counts each entry into a phase, key `flow_version:phase:attempt`; when such a phase is done the key is appended once to `.jevflow/side_effects.jsonl` (O_APPEND + fsync, torn lines skipped). The ledger outranks state.json: after a state reset the phase is restored as done. A failing check on a done side-effect phase, or an on_fail route into one, asks a human instead of re-entering it. Session context shows the key and lists performed side effects.
- F5 done: `jevflow/notify.py`: `notify: {command, on: [ask_human|goal_complete|budget], timeout_s <= 30}` validated; run from the Stop hook (ask_human, goal_complete, budget_* and hook_cap) and the supervisor (budget and api_errors exits), JSON on stdin plus JEVFLOW_EVENT/CONDITION/PHASE/MESSAGE env, key env vars scrubbed, once per (event, condition, phase), failures and timeouts journaled only. `jevflow/subgates.py` + SubagentStop / TaskCompleted hooks (`gates.subagent_stop`, `gates.task_completed`, off by default): two nouls (complete, claims_done) on the subtask text (subagent's first prompt from its transcript, or task subject + description) and its latest message; blocks only a confident premature claim (claims_done >= flag and complete <= 1 - auto), at most 2 holds per subtask, any Jev failure or missing evidence allows, internal agents with empty agent_type skipped. TaskCompleted can only block through exit code 2, so the hook returns a private code 42 that the launcher maps to 2; every other non-zero code still maps to 0.
- Live check (2 Jev calls, synthetic): premature subagent claim complete 0.13 / claims 0.89 -> block; genuine result 0.90 / 0.80 -> allow. docs/RESEARCH.md, loop/spikes/f5_live.json.
- Total suite: 278 tests, all pass on /usr/bin/python3.11 (1 skipped without key). New: tests/test_regions.py (30: parsing, forged ids, judge question/state/budget, 6-row hold table, hold never advances, review path, degraded, attempts and keys, side-effect regression/on_fail, ledger torn line/once/restore, hook ingest + hold + advance, bad files, context, state-reset restore, NEEDS_HUMAN), tests/test_notify_subgates.py (20: notify parse/event map/env without key/once/filter/failure/timeout with a lingering grandchild, Stop notify, budget once, compose table, request shape, degraded paths, transcript readers, hooks per mode, cap, internal agents, TaskCompleted exit code, launcher 42->2 only, concurrent gate write), supervisor budget notify. Mutation checks (merge removed, side-effect guard removed, sub-step hold removed) each failed tests; backups kept in loop/scratch and restored with cp.
- critic: 5 issues found, 5 fixed (a Stop saved state without the lock the gate hooks use, so a background subagent's SubagentStop or PreToolUse landing during a Stop was lost, including its hold counter, which could let the 2-hold cap be exceeded; now the Stop merges gate entries, Jev calls and holds under the lock using a journal `seq`; a side-effect phase without a check could be ledgered done on a Jev judgment alone, making "done" permanent without evidence, now `side_effect` requires `check`; notify used subprocess pipes, so a backgrounded grandchild could hang the Stop hook past the timeout, now stderr goes to a temp file and the process group is killed; the unchanged-but-warned subtasks file journaled on every Stop; an off-by-float made the gate miss complete == 0.20). Checked: no em-dashes, no personal paths in shipped dirs, key string absent from the repo.
- Fence note: one ad hoc SessionStart render check used tempfile.mkdtemp under /tmp (toy flow only) and removed it; unit tests also use /tmp temp dirs as in earlier cycles.
- Known gaps (for README): notify.command and checks come from flow.json, which the agent can edit; sub-step titles are agent text sent to Jev (they can only hold, never advance); SessionStart and StopFailure writes are still unlocked; TaskCompleted reads the transcript tail, which may lag the live turn (then it allows).
